-- a script that prints the following description of the table first_table
-- a script that prints the following description of the table first_table
CREATE TABLE hbtn_0c_0.first_table(ID INT DEFAULT NULL,
                         NAME VARCHAR(256) DEFAULT NULL);